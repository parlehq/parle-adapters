{
  "name": "@parlehq/claude-plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@parlehq/mcp-server": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-mcp-artifact.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node scripts/check-mcp-artifact.mjs && node --test test/*.test.mjs"
  }
}
